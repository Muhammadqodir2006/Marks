<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/teacher_nav"
    app:startDestination="@id/groupsFragment">

    <fragment
        android:id="@+id/groupsFragment"
        android:name="uz.itschool.marks.screen.teacher.GroupsFragment"
        android:label="fragment_groups"
        tools:layout="@layout/fragment_groups" >
        <action
            android:id="@+id/action_groupsFragment_to_marksFragment"
            app:destination="@id/marksFragment"
            app:popUpTo="@id/groupsFragment" />
    </fragment>
    <fragment
        android:id="@+id/marksFragment"
        android:name="uz.itschool.marks.screen.teacher.MarksFragment"
        android:label="fragment_marks"
        tools:layout="@layout/fragment_marks" />
</navigation>